# Server-side secret. Generate a long random value and configure it in hosting.
INGEST_TOKEN=

# Public origin used by metadata when request headers are unavailable.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
