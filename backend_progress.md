# Backend progress

Status: complete

- Cloudflare-compatible Vinext application with D1 schema and migration.
- Live OTruyen catalog/detail/chapter integration with validated IDs, domains and paths.
- Scheduled ingestion, provenance, deterministic auto-tags and conservative content ratings.
- AniList + Kitsu entity resolution and Bayesian external score aggregation with confidence.
- Authenticated ingestion/rating admin endpoints using constant-time token comparison.
- Optional D1 sync for library and reading progress with scoped idempotency keys.
- Guarded BYOK AI proxy with provider allowlist, request validation, timeout and rate limiting.

