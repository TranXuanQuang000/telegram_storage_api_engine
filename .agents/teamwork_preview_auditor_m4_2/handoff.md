# Forensic Audit Handoff Report

**Auditor**: Forensic Auditor (`teamwork_preview_auditor_m4_2`)  
**Target Project**: Muc Pet Animation & Sprite Sheet Integration  
**Project Root**: `d:/Code/Project/App Truyen Nova`  
**Verdict**: **CLEAN**  

---

## 1. Observation

### Source & Code File Inspection
- **`generate_sprite.py`** (Lines 1–192):
  - Function `remove_background_cv2()` (Lines 6–40): Uses OpenCV HSV color thresholding (`cv2.inRange(hsv, lower_grey, upper_grey)`) to convert checkerboard/grey backgrounds to transparent RGBA.
  - Function `create_sprite_sheet()` (Lines 42–190):
    - Target canvas dimensions: 1248px x 696px (6 columns x 4 rows, cell size 208px x 174px).
    - Grid layout:
      - **Row 0 (Y: 0px)**: Idle (6 frames) with vertical bobbing (`idle_y = [2, 0, -2, -2, 0, 2]`), squash/stretch (`scale_x`/`scale_y`), and spatial eye-blink array masking on frame 2 (`col == 2`).
      - **Row 1 (Y: -174px)**: Running (6 frames) with rotation tilt (`run_rot = [-2, -5, -3, 4, 1, -2]`), vertical bounce, and scale shift.
      - **Row 2 (Y: -348px)**: Petting/Happy (6 frames) with squish animation, pixel heart overlays (`draw_pixel_heart` using hot pink `#ff4f87`), and sparkle overlays (`draw_sparkle` using yellow `#ffe664`).
      - **Row 3 (Y: -522px)**: Dragging (6 frames) with dangling gravity stretch (`scale_y` ~1.08–1.10) and sway rotation (`drag_rot = [-6, -3, 1, 5, 2, -2]`).
    - Frame resizing uses nearest-neighbor interpolation (`Image.NEAREST`), maintaining sharp pixel art aesthetic.

- **`public/muc-pet-sprite.png`** (Empirical Inspection via PIL & NumPy):
  - Image size: 1248 x 696 pixels, Mode: `RGBA`, Format: `PNG`.
  - Transparent background pixels: 489,134 (`alpha == 0`).
  - Opaque pet pixels: 379,474 (`alpha > 0`).
  - Frame pixel variance check:
    - Row 0 (Idle): Opaque pixel counts range 17,145–17,413. Frame 2 eye blink contains 17,145 pixels (268 pixels masked out for blink). Pixel diff vs Col 0 is non-zero across all offset columns.
    - Row 1 (Running): Frame pixel diffs vs Col 0 range from 2,141,964 to 4,443,157.
    - Row 2 (Petting): Frame pixel diffs vs Col 0 range from 1,162,646 to 5,037,615 (heart/sparkle drawings present).
    - Row 3 (Dragging): Frame pixel diffs vs Col 0 range from 2,427,223 to 4,642,624.

- **`app/globals.css`** (Lines 683–755, 1210–1212):
  - Class `.muc-pet__sprite-anim`: `width: 208px; height: 174px; background-size: 1248px 696px; image-rendering: pixelated; animation: playSprite 0.3s steps(6) infinite;`.
  - Keyframe `@keyframes playSprite`: `100% { background-position-x: -1248px; }`.
  - Y-Offsets:
    - Idle (`.is-idle` or fallback): `background-position-y: 0px;`
    - Running (`.is-moving`): `background-position-y: -174px;`
    - Petting (`.is-petting`): `background-position-y: -348px;`
    - Dragging (`.is-dragging`): `background-position-y: -522px;`

- **`components/MucPet.tsx`** (Lines 1–259):
  - Implements React client component managing state for `moving`, `dragging`, `quiet`, `mood` (`idle` vs `petting`).
  - Applies CSS classes `.is-moving`, `.is-dragging`, `.is-petting`, `.is-reader` to `.muc-pet` wrapper.
  - Renders `<div className="muc-pet__sprite-anim" />`.

- **Build Verification**:
  - Command: `npm run build` (`vinext build`).
  - Result: Completed successfully with 0 errors (all RSC/SSR/Client chunks built cleanly).

---

## 2. Logic Chain

1. **Hardcode & Facade Check**:
   - Analyzed `generate_sprite.py`, `app/globals.css`, and `components/MucPet.tsx`.
   - No hardcoded test strings, dummy return constants, or mock assertions were present.
   - All 24 frames of the sprite sheet are dynamically generated by mathematical transform matrices (squash, stretch, rotation, spatial pixel manipulation, custom drawing methods) operating on the raw pixel art frame.

2. **Asset Integrity & Dimensions**:
   - The output file `public/muc-pet-sprite.png` was empirically loaded into Python's `PIL` and `numpy`.
   - Shape verified: 696 rows x 1248 columns x 4 RGBA channels.
   - Cell dimensions 208px x 174px match CSS rules 1:1.
   - Alpha channel contains 489,134 transparent pixels, confirming proper background keying without black/checkerboard borders.

3. **Animation Matrix & CSS Harmony**:
   - Row 0 (0px) -> Idle (6 frames, `steps(6)`).
   - Row 1 (-174px) -> Running (6 frames, `steps(6)`).
   - Row 2 (-348px) -> Petting (6 frames, `steps(6)`).
   - Row 3 (-522px) -> Dragging (6 frames, `steps(6)`).
   - The keyframe `-1248px` divided by 6 steps yields exact 208px increments per step (`0px`, `-208px`, `-416px`, `-624px`, `-832px`, `-1040px`).

4. **Forensic Integrity Verification**:
   - Zero facade patterns detected.
   - Zero pre-populated test cheats or hardcoded mock files.
   - Genuine Python script execution produces valid pixel art outputs.
   - Build environment validates cleanly with Next.js compiler.

---

## 3. Caveats

- **Environment Dependencies**: Running `generate_sprite.py` requires Python 3 with `pillow`, `opencv-python` (`cv2`), and `numpy` installed.
- **Browser Sub-pixel Scaling**: High DPI screens or non-100% zoom levels might introduce slight sub-pixel anti-aliasing in browsers unless `image-rendering: pixelated` is strictly honored by the user's browser rendering engine.

---

## 4. Conclusion

The work product passes ALL forensic integrity checks with **ZERO VIOLATIONS**:
- **Hardcoded test results / Facades**: 0 detected.
- **Authentic implementation**: 100% genuine algorithmic sprite rendering script and React/CSS component integration.
- **Asset resolution & RGBA validity**: Verified (1248x696 RGBA, transparent background).
- **CSS Keyframes**: Verified mathematically exact (`steps(6)`, 208px cell width, -174px Y-offsets).
- **Project Build**: Verified (`npm run build` completed with 0 errors).

Final Verdict: **CLEAN**

---

## 5. Verification Method

To independently verify this audit, execute the following commands in PowerShell from the project root `d:/Code/Project/App Truyen Nova`:

1. **Re-generate Sprite Sheet**:
   ```powershell
   python generate_sprite.py
   ```
   *Expected output*: `Successfully generated sprite sheet: ...\public\muc-pet-sprite.png` | `Dimensions: 1248x696 (6 cols x 4 rows), Mode: RGBA`.

2. **Empirical Image & Matrix Inspection**:
   ```powershell
   python -c "from PIL import Image; import numpy as np; img=Image.open('public/muc-pet-sprite.png'); arr=np.array(img); assert img.size==(1248,696); assert img.mode=='RGBA'; assert np.count_nonzero(arr[:,:,3]==0)>0; print('IMAGE MATRIX VALID')"
   ```
   *Expected output*: `IMAGE MATRIX VALID`.

3. **Project Build Check**:
   ```powershell
   npm run build
   ```
   *Expected output*: `Build complete.` (0 errors).
